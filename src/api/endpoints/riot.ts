import express = require("express");
import APIWebServer from "../server";

// GET '/api/lookup/:region/:name'
export function summonerLookup(this: APIWebServer, req: express.Request, res: express.Response) {

}

// GET '/api/verify/:region/:id/:expected'
export function runePageVerify(this: APIWebServer, req: express.Request, res: express.Response) {

}